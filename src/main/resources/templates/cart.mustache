<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Your Cart</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>

<h1>Your Cart</h1>

{{#cartItems.length}}
    <table>
        <thead>
        <tr>
            <th>Product</th>
            <th>Code</th>
            <th>Price</th>
            <th>Quantity</th>
            <th>Total</th>
            <th>Action</th>
        </tr>
        </thead>

        <tbody>
        {{#cartItems}}
            <tr>
                <td>{{name}}</td>
                <td>{{code}}</td>
                <td>S${{price}}</td>
                <td>{{quantity}}</td>
                <td>S${{total}}</td>
                <td>
                    <form method="post" action="/cart/remove/{{code}}">
                        <button type="submit">Remove</button>
                    </form>
                </td>
            </tr>
        {{/cartItems}}
        </tbody>
    </table>

    <h3>Total Amount: S${{grandTotal}}</h3>

    <form method="post" action="/cart/checkout">
        <button type="submit">Checkout</button>
    </form>

{{/cartItems.length}}

{{^cartItems.length}}
    <p>Your cart is empty.</p>
{{/cartItems.length}}

<br>
<a href="/home">Back to Home</a>

</body>
</html>