<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Your Cart</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>

<h1>Your Cart</h1>
{{^isEmpty}}
<table>
    <thead>
    <tr>
        <th>Product</th>
        <th>Code</th>
        <th>Price</th>
        <th>Quantity</th>
        <th>Total</th>
        <th>Action</th>
    </tr>
    </thead>

    <tbody>
    {{#cartItems}}
        <tr>
            <td>{{product.name}}</td>
            <td>{{product.code}}</td>
            <td>S${{product.price}}</td>
            <td>{{quantity}}</td>
            <td>S${{totalPrice}}</td>
            <td>
                <form method="delete" action="/cart-item">
                    <input type="hidden" name="id" value="{{id}}">
                    <button type="submit">Remove</button>
                </form>
            </td>
        </tr>
    {{/cartItems}}
    </tbody>
</table>

<h3>Total Amount: S${{grandTotal}}</h3>

<form method="post" action="/cart/checkout">
    <button type="submit">Checkout</button>
</form>
{{/isEmpty}}

{{#isEmpty}}
    <p>Your cart is empty.</p>
{{/isEmpty}}

<br>
<a href="/">Back to Home</a>

</body>
</html>